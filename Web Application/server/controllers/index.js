const todos = require('./todos');
const todoItems = require('./todoitems');
const users = require('./users');
const uploads = require('./uploads');

module.exports = {
  todos, todoItems, users, uploads
};